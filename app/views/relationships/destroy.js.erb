$("#follow_count").html("<%= link_to @user.follows.count, user_follows_path(@user) %>");
$("#follower_count").html("<%= link_to @user.followers.count, user_followers_path(@user) %>");
$("#follow_btn_<%= @user.id %>").html("<%= j(render "users/info", user: @user) %>");